[project]
name = "miryoku-vial"
version = "0.1.0"
description = "Generate a Vial .vil Miryoku keymap for the Bad Wings V2 keyboard"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
